import { render, screen } from '@testing-library/react'

import { {{pascalCase name}} } from '.'

describe('<{{pascalCase name}} />', () => {
  it('should be able to render the {{pascalCase name}}', () => {
    render(<{{pascalCase name}} />)
  })
})
